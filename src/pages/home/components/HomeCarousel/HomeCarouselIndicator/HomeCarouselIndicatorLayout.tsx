import {LayoutProps} from "@/types/LayoutProps";

const HomeCarouselIndicatorLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default HomeCarouselIndicatorLayout;