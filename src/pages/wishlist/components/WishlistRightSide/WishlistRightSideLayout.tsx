import {LayoutProps} from "@/types/LayoutProps";

const WishlistRightSideLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div className={""}>
            {children}
        </div>
    )
}

export default WishlistRightSideLayout;