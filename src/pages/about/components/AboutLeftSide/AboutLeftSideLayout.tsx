import {LayoutProps} from "@/types/LayoutProps";

const AboutLeftSideLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default AboutLeftSideLayout;