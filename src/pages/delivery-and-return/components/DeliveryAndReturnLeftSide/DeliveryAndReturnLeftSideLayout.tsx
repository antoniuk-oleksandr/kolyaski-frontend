import {LayoutProps} from "@/types/LayoutProps";

const DeliveryAndReturnLeftSideLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default DeliveryAndReturnLeftSideLayout;