import {LayoutProps} from "@/types/LayoutProps";

const NewProductRightCategorySideLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default NewProductRightCategorySideLayout;