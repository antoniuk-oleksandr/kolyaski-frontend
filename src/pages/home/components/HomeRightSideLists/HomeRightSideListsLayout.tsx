import {LayoutProps} from "@/types/LayoutProps";


const HomeRightSideListsLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default HomeRightSideListsLayout;